# Nueva asignatura

Repositorio base para desplegar contenidos de la asignatura con **MkDocs**.

## Estructura
- `docs/` contiene las páginas en Markdown.
- `mkdocs.yml` define el menú de navegación.
- `site/` (opcional) es el resultado de `mkdocs build` (no se recomienda versionarlo).
